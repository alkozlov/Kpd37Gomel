import { MetisMenuDirective } from './metis-menu.directive';

describe('MetisMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new MetisMenuDirective();
    expect(directive).toBeTruthy();
  });
});
